<h1>The Longest Word Game</h1>
<p><em>Make the longest English word you can with these letters:</em></p>

<% @letters.each { |letter| %><p class="letter"><%= letter %></p><% } %>

<form action="/score" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :grid, @letters %>
  <input class="form-style" type="text" name="word" placeholder="Type the longest word you can!">
  <input type="submit" value="Done!">
</form>
